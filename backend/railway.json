{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '== migrate ==' && npx prisma migrate deploy; echo '== starting server ==' && node dist/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
